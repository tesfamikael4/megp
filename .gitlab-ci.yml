stages:
  - triggers
trigger_project1:
  stage: triggers
  trigger:
    include: apps/back-office/+demo-fe/.gitlab-ci.yml
  rules:
    - changes:
        - apps/**/*
trigger_project2:
  stage: triggers
  trigger:
    include: apps/portal/egp/.gitlab-ci.yml
  rules:
    - changes:
        - apps/**/*
