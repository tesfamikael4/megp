stages:
  - triggers
trigger_portal:
  stage: triggers
  trigger:
    include: portal/egp/.gitlab-ci.yml
  rules:
    - changes:
        - apps/**/*
trigger_back-office:
  stage: triggers
  trigger:
    include: back-office/+demo-fe/.gitlab-ci.yml
  rules:
    - changes:
        - apps/**/*/*

