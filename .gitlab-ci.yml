stages:
  - triggers
trigger_back-office:
  stage: triggers
  trigger:
    include: apps/back-office/.gitlab-ci.yml
  rules:
    - changes:
        - apps/**/*
trigger_portal:
  stage: triggers
  trigger:
    include: apps/portal/egp/.gitlab-ci.yml
  rules:
    - changes:
        - apps/**/*
