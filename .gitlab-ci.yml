image: node:latest
stages:
  - merge
  - build
  - deploy

merge:
  stage: merge
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
  before_script:
    - corepack enable
    - corepack prepare pnpm@latest-8 --activate
    - pnpm config set store-dir .pnpm-store
  script:
    - pnpm install
    - pnpm -w run turbo run lint  --filter "...[origin/main...HEAD^]"
    - pnpm -w run turbo run build  --filter "...[origin/main...HEAD^]"
  cache:
    key:
      files:
        - pnpm-lock.yaml
    paths:
      - .pnpm-store

build:
  stage: build
  image: docker:latest
  services:
    - docker:dind

  rules:
    - if: $CI_COMMIT_BRANCH == "main" && $CI_PIPELINE_SOURCE == "push"
  before_script:
    - apk --update add nodejs npm git
    
    - npm install -g pnpm
    
    # - corepack enable
    # - corepack prepare pnpm@latest-8 --activate
    #- pnpm config set store-dir .pnpm-store
    
  script:
    # - pnpm install
    # - pnpm -w run turbo run lint  --filter "...[origin/main...HEAD^]"
    # - pnpm -w run turbo run build:docker  --filter "...[origin/main...HEAD^]"
    # - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - echo $CI_REGISTRY_IMAGE
    # - docker tag iam-fe:latest $CI_REGISTRY_IMAGE/iam-fe:latest
    # - docker push $CI_REGISTRY_IMAGE/iam-fe:latest
  cache:
    key:
      files:
        - pnpm-lock.yaml
    paths:
      - .pnpm-store

deploy:
  stage: deploy
  image: docker:latest
  rules:
    - if: $CI_COMMIT_BRANCH == "main" && $CI_PIPELINE_SOURCE == "push"
  # before_script:
  #   - apk --update add curl
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker pull $CI_REGISTRY_IMAGE/iam-fe:latest
    - docker container rm -f iam-fe || true
    - docker run -d -p 5511:5511 --name iam-fe $CI_REGISTRY_IMAGE/iam-fe:latest
