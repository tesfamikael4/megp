export interface Taxonomy {
  id: string;
  title: string;
  key: string;
  code: string;
  definition: string;
  synonym?: string;
  acronym: string;
  name: string;
  version: string;
  upload: string;
  parentId: string;
}
