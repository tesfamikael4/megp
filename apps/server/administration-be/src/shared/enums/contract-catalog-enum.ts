export enum ContractCatalogStatus {
  Active = 'Active',
  Inactive = 'Inactive',
  Draft = 'Draft',
}

export enum ContractItemStatus {
  Active = 'Active',
  Inactive = 'Inactive',
  Utilized = 'Utilized',
  Cancelled = 'Cancelled',
}

export enum ContractBeneficiaryStatus {
  Active = 'Active',
  Inactive = 'Inactive',
  Draft = 'Draft',
}
