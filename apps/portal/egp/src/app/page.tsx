import { Dashboard } from '@/shared/dashboard/dashboard';
import styles from './page.module.scss';
export default function Home() {
  return (
    <div>
      <header className={styles.header}>dd</header>
      <button> button</button>
      <Dashboard />
    </div>
  );
}
